# سندات القبض - Receipt Vouchers

## نظرة عامة

سندات القبض هي نوع من السندات المحاسبية المستخدمة لتسجيل عمليات القبض النقدية. في هذا النظام، سندات القبض لها نوع السند `vouch_type = 1`.

## البنية التقنية

### الجداول المستخدمة

1. **vouchers** - الجدول الرئيسي للسند
2. **voucher_box** - تفاصيل الصناديق المستخدمة في السند
3. **voucher_dtl** - تفاصيل الحسابات المحاسبية

### API Endpoints

#### السند الرئيسي
- `GET vouchers_list` - قائمة السندات
- `POST api_create_vouch` - إنشاء سند جديد
- `PUT api_update_vouch/<int:id>` - تحديث سند موجود
- `DELETE api_delete_vouch/<int:id>` - حذف سند

#### تفاصيل الصناديق
- `GET vouchers_box_list` - قائمة تفاصيل الصناديق
- `POST api_create_vouch_box` - إنشاء تفصيل صندوق جديد
- `PUT api_update_vouch_box/<int:id>` - تحديث تفصيل صندوق
- `DELETE api_delete_vouch_box/<int:id>` - حذف تفصيل صندوق

#### تفاصيل الحسابات
- `GET vouchers_dtl_list` - قائمة تفاصيل الحسابات
- `POST api_create_vouch_dtl` - إنشاء تفصيل حساب جديد
- `PUT api_update_vouch_dtl/<int:id>` - تحديث تفصيل حساب
- `DELETE api_delete_vouch_dtl/<int:id>` - حذف تفصيل حساب

## الصفحات المتاحة

### 1. صفحة إنشاء/تعديل سند القبض
**المسار:** `/dashboard/forms/receipt_voucher`

**الميزات:**
- إنشاء سند قبض جديد
- تعديل سند موجود
- إدارة تفاصيل الصناديق
- إدارة تفاصيل الحسابات
- التحقق من توازن السند
- طباعة السند
- التنقل بين السندات

### 2. صفحة قائمة سندات القبض
**المسار:** `/dashboard/receipt_vouchers`

**الميزات:**
- عرض جميع سندات القبض
- البحث والتصفية
- إحصائيات السندات
- إجراءات العرض والتعديل والحذف
- تصدير وطباعة التقارير

## كيفية الاستخدام

### إنشاء سند قبض جديد

1. انتقل إلى **نظام الحسابات > النماذج > سند قبض**
2. املأ البيانات الأساسية:
   - تاريخ السند
   - رقم المرجع (اختياري)
   - البيان (اسم المستلم)
   - حالة السند

3. أضف تفاصيل الصناديق:
   - اختر الصندوق
   - أدخل المبلغ قبل الضريبة
   - أدخل نسبة الضريبة (اختياري)
   - أدخل قيمة الضريبة (اختياري)
   - الإجمالي سيحسب تلقائياً

4. أضف تفاصيل الحسابات:
   - اختر الحساب المحاسبي
   - أدخل المبلغ في عمود الدائن
   - أدخل البيان (اختياري)

5. تأكد من توازن السند:
   - إجمالي الصناديق = إجمالي الدائن
   - إجمالي المدين = 0

6. اضغط **حفظ** لحفظ السند

### تعديل سند موجود

1. انتقل إلى **نظام الحسابات > التقارير > سندات القبض**
2. ابحث عن السند المطلوب
3. اضغط على أيقونة **تعديل**
4. قم بالتعديلات المطلوبة
5. اضغط **حفظ**

### حذف سند

1. انتقل إلى **نظام الحسابات > التقارير > سندات القبض**
2. ابحث عن السند المطلوب
3. اضغط على أيقونة **حذف**
4. أكد الحذف

## القواعد المحاسبية

### توازن السند
- يجب أن يتساوى إجمالي الصناديق مع إجمالي الدائن
- إجمالي المدين يجب أن يكون صفر
- لا يمكن حفظ السند إذا لم يكن متوازناً

### أنواع الحسابات
- في سند القبض، يتم إدخال المبالغ في عمود **الدائن** فقط
- الحسابات المستخدمة عادة:
  - البنوك (للإيداع)
  - الصندوق (للنقد)
  - حسابات المدينين (للقبض من العملاء)

## الميزات المتقدمة

### التنقل بين السندات
- استخدام أزرار التنقل (أول، سابق، تالي، أخير)
- عرض رقم السند الحالي والوقت

### الطباعة
- طباعة السند بتنسيق احترافي
- يتضمن جميع التفاصيل والإجماليات

### البحث والتصفية
- البحث بالكلمات المفتاحية
- التصفية حسب الحالة
- التصفية حسب التاريخ

## الأمان والصلاحيات

- يتطلب تسجيل الدخول
- التحقق من صحة البيانات قبل الحفظ
- تأكيد الحذف لمنع الحذف العرضي

## استكشاف الأخطاء

### مشاكل شائعة

1. **خطأ "يجب أن يتساوى إجمالي الصناديق مع إجمالي الدائن"**
   - تأكد من إدخال المبالغ الصحيحة
   - تحقق من عدم وجود أخطاء في الحساب

2. **لا يمكن حفظ السند**
   - تأكد من إدخال جميع البيانات المطلوبة
   - تحقق من اتصال الإنترنت

3. **لا تظهر الصناديق في القائمة**
   - تأكد من وجود صناديق في النظام
   - تحقق من صلاحيات الوصول

## الدعم الفني

في حالة مواجهة أي مشاكل، يرجى التواصل مع فريق الدعم الفني مع تقديم:
- رقم السند (إن وجد)
- وصف المشكلة
- لقطة شاشة للخطأ (إن وجدت)
