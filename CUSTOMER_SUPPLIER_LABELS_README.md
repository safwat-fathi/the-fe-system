# تغيير كلمة "عميل" إلى "مورد" في فواتير الشراء

## المشكلة
في فواتير الشراء، كانت كلمة "عميل" تظهر بدلاً من "مورد" مما يسبب لبساً في المصطلحات.

## الحل المطبق

### 1. **إضافة خاصية `invoiceType`**
تم إضافة خاصية `invoiceType` إلى مكون `InvoiceSelectors` لتحديد نوع الفاتورة:
```typescript
invoiceType?: "purchase" | "sale" | "purchase_return" | "sale_return";
```

### 2. **تغيير النصوص بناءً على نوع الفاتورة**
تم تعديل النصوص لتظهر "مورد" في فواتير الشراء و"عميل" في فواتير البيع:

#### في مكون `InvoiceSelectors`:
```typescript
// عنوان الحقل
{invoiceType === "purchase" || invoiceType === "purchase_return" ? "المورد:" : "العميل:"}

// النص التوضيحي
placeholder={invoiceType === "purchase" || invoiceType === "purchase_return" ? "اختر المورد..." : "اختر العميل..."}

// رسالة الخطأ
`${invoiceType === "purchase" || invoiceType === "purchase_return" ? "مورد" : "عميل"} رقم ${selectedCustomer}`

// عنوان فواتير العميل/المورد
فواتير {invoiceType === "purchase" || invoiceType === "purchase_return" ? "المورد" : "العميل"}:

// رسالة اختيار العميل/المورد
اختر {invoiceType === "purchase" || invoiceType === "purchase_return" ? "مورداً" : "عميلاً"} لعرض معلومات العنوان
```

### 3. **تغيير رسائل الخطأ**
تم تغيير رسائل الخطأ في صفحات فواتير الشراء:
```typescript
// قبل التغيير
toast.error("يرجى اختيار العميل");

// بعد التغيير
toast.error("يرجى اختيار المورد");
```

## الملفات المعدلة

### 1. `components/InvoiceSelectors.tsx`
- إضافة خاصية `invoiceType`
- تعديل جميع النصوص لتظهر "مورد" أو "عميل" حسب نوع الفاتورة

### 2. `app/dashboard/forms/invoices/Gold_invoice1/page.tsx`
- تغيير رسائل الخطأ من "عميل" إلى "مورد"
- إضافة `invoiceType="purchase"`

### 3. `app/dashboard/forms/invoices/Gold_invoice3/page.tsx`
- تغيير رسائل الخطأ من "عميل" إلى "مورد"
- إضافة `invoiceType="purchase_return"`

### 4. `app/dashboard/forms/invoices/Gold_invoice2/page.tsx`
- إضافة `invoiceType="sale"` (للتوضيح)

### 5. `app/dashboard/forms/invoices/Gold_invoice4/page.tsx`
- إضافة `invoiceType="sale_return"` (للتوضيح)

## أنواع الفواتير

### فواتير الشراء (تظهر "مورد"):
- **`Gold_invoice1`**: فاتورة شراء
- **`Gold_invoice3`**: فاتورة مردود شراء

### فواتير البيع (تظهر "عميل"):
- **`Gold_invoice2`**: فاتورة بيع
- **`Gold_invoice4`**: فاتورة مردود بيع

## النصوص المتغيرة

### في فواتير الشراء:
- "المورد:" بدلاً من "العميل:"
- "اختر المورد..." بدلاً من "اختر العميل..."
- "مورد رقم X" بدلاً من "عميل رقم X"
- "فواتير المورد:" بدلاً من "فواتير العميل:"
- "اختر مورداً لعرض معلومات العنوان" بدلاً من "اختر عميلاً لعرض معلومات العنوان"
- "يرجى اختيار المورد" بدلاً من "يرجى اختيار العميل"

### في فواتير البيع:
- تبقى جميع النصوص كما هي ("عميل")

## الفوائد

### 1. **دقة المصطلحات**
- استخدام المصطلح الصحيح لكل نوع فاتورة
- وضوح في العلاقة التجارية

### 2. **تجربة مستخدم محسنة**
- واجهة واضحة ومفهومة
- رسائل خطأ دقيقة

### 3. **مرونة في التطوير**
- سهولة إضافة أنواع فواتير جديدة
- إمكانية تخصيص النصوص حسب الحاجة

## كيفية الاختبار

### 1. **فاتورة شراء (Gold_invoice1)**
- افتح فاتورة شراء جديدة
- تأكد من ظهور "المورد:" في العنوان
- تأكد من ظهور "اختر المورد..." في placeholder
- جرب عدم اختيار مورد وحفظ الفاتورة
- تأكد من ظهور رسالة "يرجى اختيار المورد"

### 2. **فاتورة مردود شراء (Gold_invoice3)**
- افتح فاتورة مردود شراء جديدة
- تأكد من ظهور "المورد:" في العنوان
- تأكد من ظهور "اختر المورد..." في placeholder

### 3. **فاتورة بيع (Gold_invoice2)**
- افتح فاتورة بيع جديدة
- تأكد من ظهور "العميل:" في العنوان
- تأكد من ظهور "اختر العميل..." في placeholder

### 4. **فاتورة مردود بيع (Gold_invoice4)**
- افتح فاتورة مردود بيع جديدة
- تأكد من ظهور "العميل:" في العنوان
- تأكد من ظهور "اختر العميل..." في placeholder

## ملاحظات مهمة

1. **التوافق**: جميع فواتير الشراء تستخدم "مورد"
2. **الاتساق**: جميع فواتير البيع تستخدم "عميل"
3. **المرونة**: يمكن إضافة أنواع فواتير جديدة بسهولة
4. **الأداء**: لا يوجد تأثير على الأداء، فقط تغيير نصوص
