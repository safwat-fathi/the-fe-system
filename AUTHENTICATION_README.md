# نظام المصادقة الجديد - نفيس Web

## نظرة عامة

تم تحديث نظام نفيس Web لاستخدام نظام مصادقة قوي يعتمد على التوكن (Token-based Authentication) بدلاً من النظام القديم.

## الميزات الجديدة

### 1. نظام تسجيل الدخول المحسن
- استخدام API حقيقي لتسجيل الدخول: `http://149.102.143.102:8000/api/login/`
- حذف حقول "السنة المالية" و "اختيار الفرع" من شاشة تسجيل الدخول
- التحقق من صحة بيانات المستخدم قبل الدخول

### 2. حماية الصفحات
- جميع الصفحات محمية بواسطة middleware
- التحقق التلقائي من وجود التوكن في كل طلب
- إعادة توجيه تلقائي لصفحة تسجيل الدخول إذا لم يكن هناك توكن صالح

### 3. إدارة التوكن
- حفظ التوكن في localStorage و cookies
- إضافة التوكن تلقائياً لجميع طلبات API
- حذف التوكن عند الخروج من النظام

## كيفية الاستخدام

### تسجيل الدخول
1. افتح النظام على الرابط: `http://localhost:3000`
2. أدخل اسم المستخدم وكلمة المرور
3. اضغط "دخول النظام"
4. سيتم توجيهك تلقائياً للداشبورد

### الخروج من النظام
- اضغط على زر "خروج" في القائمة الجانبية
- سيتم حذف التوكن وإعادة توجيهك لصفحة تسجيل الدخول

## الملفات المحدثة

### ملفات جديدة
- `middleware.ts` - middleware للتحقق من المصادقة
- `components/AuthGuard.tsx` - مكون حماية الصفحات
- `components/LogoutButton.tsx` - زر الخروج من النظام

### ملفات محدثة
- `app/page.tsx` - صفحة تسجيل الدخول الجديدة
- `utilities/api.ts` - إضافة وظائف المصادقة
- `app/dashboard/layout.tsx` - إضافة AuthGuard وزر الخروج

## API Endpoints

### تسجيل الدخول
```
POST http://149.102.143.102:8000/api/login/
Content-Type: application/json

{
  "username": "admin",
  "password": "admin_123"
}
```

### الاستجابة المتوقعة
```json
{
  "success": true,
  "message": "Login successful",
  "token": "0bae912399f2298727b58b3a49e7dee7be27a103"
}
```

## الأمان

- التوكن صالح لمدة 24 ساعة
- يتم حذف التوكن تلقائياً عند انتهاء الصلاحية
- جميع طلبات API تتضمن التوكن في header
- حماية ضد الوصول غير المصرح للصفحات

## استكشاف الأخطاء

### إذا لم يتم تسجيل الدخول
1. تأكد من صحة اسم المستخدم وكلمة المرور
2. تحقق من اتصال الإنترنت
3. تأكد من أن API server يعمل
4. افتح Developer Tools (F12) وتحقق من Console للأخطاء
5. تحقق من Network tab لرؤية طلبات API

### إذا تم إعادة التوجيه لصفحة تسجيل الدخول
1. تحقق من أن التوكن لم ينتهي
2. جرب تسجيل الدخول مرة أخرى
3. تحقق من إعدادات المتصفح (cookies)

### رسائل الخطأ الشائعة
- **"لا يمكن الاتصال بالخادم"**: تحقق من اتصال الإنترنت
- **"استجابة غير صحيحة من الخادم"**: مشكلة في API server
- **"خطأ في الاتصال: 401"**: بيانات تسجيل دخول خاطئة
- **"خطأ في الاتصال: 500"**: خطأ في الخادم

## ملاحظات تقنية

- يستخدم النظام Next.js 15 مع App Router
- التوكن محفوظ في localStorage و cookies للتوافق مع middleware
- جميع الطلبات تتضمن header: `Authorization: Token {token}`
- يتم التعامل مع أخطاء 401 تلقائياً بحذف التوكن وإعادة التوجيه
