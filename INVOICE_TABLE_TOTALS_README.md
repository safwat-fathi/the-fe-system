# تعديل حقول الإجماليات في جدول تفاصيل الفاتورة

## المشكلة
كانت حقول "اجمالي القيمة" و "اجمالي الاجور" في جدول تفاصيل الفاتورة تعرض قيماً ثابتة ولا تقوم بإعادة حساب الأسعار عند تعديلها.

## الحل المطبق

### 1. تحديث عرض الحقول
تم تعديل حقول الإجماليات لتعرض القيم المحسوبة مباشرة:
- **اجمالي القيمة**: `item.weight * item.price`
- **اجمالي الاجور**: `item.weight * item.price_w`

### 2. إعادة حساب الأسعار عند التعديل
عند تعديل أي من الإجماليات:
- **تعديل اجمالي القيمة**: يتم إعادة حساب `سعر الجرام = الإجمالي الجديد ÷ الوزن`
- **تعديل اجمالي الاجور**: يتم إعادة حساب `سعر الأجور = الإجمالي الجديد ÷ الوزن`

### 3. إعادة حساب الضريبة والإجمالي
بعد تعديل الإجماليات:
- يتم إعادة حساب الضريبة بناءً على الإجمالي الجديد
- يتم تحديث "الاجمالي شامل الضريبة" تلقائياً

## الملف المعدل

### `components/InvoiceItemTable.tsx`

#### التعديلات في دوال المعالجة:

```typescript
// دالة معالجة تغيير اجمالي القيمة
const handleTotalAChange = (index: number, value: any) => {
  const num = parseFloat(value) || 0;
  const updated = [...invoiceItems];
  
  if (updated[index]) {
    updated[index].total_a = num;
    
    // إعادة حساب سعر الجرام بناءً على الإجمالي الجديد
    if (updated[index].weight && updated[index].weight > 0) {
      updated[index].price = num / updated[index].weight;
    }
    
    // إعادة حساب الإجمالي الكلي
    updated[index].total = (updated[index].total_a || 0) + (updated[index].total_w || 0);
    
    // إعادة حساب الضريبة
    const base = (updated[index].total_a || 0) + (updated[index].total_w || 0) - (updated[index].item_disc_amt ?? 0);
    updated[index].tax = base * ((updated[index].tax_prc ?? 15) / 100);
    
    setInvoiceItems(updated);
  }
};

// دالة معالجة تغيير اجمالي الأجور
const handleTotalWChange = (index: number, value: any) => {
  const num = parseFloat(value) || 0;
  const updated = [...invoiceItems];
  
  if (updated[index]) {
    updated[index].total_w = num;
    
    // إعادة حساب سعر الأجور بناءً على الإجمالي الجديد
    if (updated[index].weight && updated[index].weight > 0) {
      updated[index].price_w = num / updated[index].weight;
    }
    
    // إعادة حساب الإجمالي الكلي
    updated[index].total = (updated[index].total_a || 0) + (updated[index].total_w || 0);
    
    // إعادة حساب الضريبة
    const base = (updated[index].total_a || 0) + (updated[index].total_w || 0) - (updated[index].item_disc_amt ?? 0);
    updated[index].tax = base * ((updated[index].tax_prc ?? 15) / 100);
    
    setInvoiceItems(updated);
  }
};
```

#### التعديلات في عرض الحقول:

```typescript
// حقل اجمالي القيمة
value={parseFloat((item.weight * item.price).toFixed(totalADigits))}

// حقل اجمالي الأجور  
value={parseFloat((item.weight * (item.price_w || 0)).toFixed(totalWDigits))}
```

## كيفية الاستخدام

### تعديل اجمالي القيمة:
1. في جدول تفاصيل الفاتورة، ابحث عن عمود "اجمالي القيمة"
2. أدخل القيمة المطلوبة في الحقل
3. سيتم إعادة حساب سعر الجرام تلقائياً
4. ستتم إعادة حساب الضريبة والإجمالي النهائي

### تعديل اجمالي الأجور:
1. في جدول تفاصيل الفاتورة، ابحث عن عمود "اجمالي الاجور"
2. أدخل القيمة المطلوبة في الحقل
3. سيتم إعادة حساب سعر الأجور تلقائياً
4. ستتم إعادة حساب الضريبة والإجمالي النهائي

## الصيغ المستخدمة

### إعادة حساب سعر الجرام:
```
سعر الجرام الجديد = اجمالي القيمة المدخل ÷ الوزن
```

### إعادة حساب سعر الأجور:
```
سعر الأجور الجديد = اجمالي الأجور المدخل ÷ الوزن
```

### إعادة حساب الضريبة:
```
الضريبة = (اجمالي القيمة + اجمالي الأجور - الخصم) × نسبة الضريبة
```

## ملاحظات مهمة

1. **الوزن المطلوب**: يجب أن يكون الوزن أكبر من صفر لإعادة حساب الأسعار
2. **التوافق**: يعمل مع جميع أنواع الدفع (ذهب فقط، أجور فقط، ذهب وأجور)
3. **التحديث التلقائي**: جميع الحقول المرتبطة تتحدث تلقائياً
4. **الخصم**: يتم خصم قيمة الخصم من الإجمالي قبل حساب الضريبة

## الفوائد

- **سهولة الاستخدام**: يمكن تعديل الإجماليات مباشرة من الجدول
- **دقة الحسابات**: إعادة حساب تلقائي لجميع القيم المرتبطة
- **توفير الوقت**: لا حاجة لحساب الأسعار يدوياً
- **تقليل الأخطاء**: الحسابات تتم تلقائياً بدقة
